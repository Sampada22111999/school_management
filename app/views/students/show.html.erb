<p style="color: green"><%= notice %></p>
<div class="d-flex align-items-center justify-content-between mb-5">
  <%= link_to "Edit this student", edit_student_path(@student), class: 'button-4' %> 
  <%= link_to "Back to students", students_path, class: 'button-4' %>
</div>
<%= render @student %>

<div class="mt-5">
  <%= button_to "Destroy this student", @student, method: :delete, class: 'button-4' %>
</div>
